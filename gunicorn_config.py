workers = 1  # Reduce workers to 1
threads = 2  # Use threads instead of processes
worker_class = 'gthread'  # Threaded workers to reduce memory usage
timeout = 120  # Increase timeout for long-running tasks